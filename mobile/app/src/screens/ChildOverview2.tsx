/**
 * `mobile/child-overview-2.png`.
 *
 * The same child hub, captured scrolled to the tile grid. One screen, one implementation - a fork would drift.
 */
export { default } from './ChildOverview';
