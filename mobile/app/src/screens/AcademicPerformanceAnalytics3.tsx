/**
 * `mobile/academic-performance-analytics-3.png` — byte-identical to `academic-performance-analytics.png`.
 *
 * An alias, not a copy. Two files with the same md5 are one screen exported
 * twice by the design tool, and forking them would give the same screen two
 * implementations that drift apart.
 */
export { default } from './AcademicPerformanceAnalytics';
